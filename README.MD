
<!-- git init - to initialize repository
git branch -M main - to set main as default branch (its master by default)
git add . - add all the files which needs to go inside commit
git commit -m "" - message
git remote add origin url - to set our local to remote git repository(once),
git push origin main -  now I can push my code to git repository/ origin main




github is a place which can host client repositries

you can make your code production ready by  optimizing/ bundling/minifying the code
npm create is already ignited, it creates basic prod ready app

-->